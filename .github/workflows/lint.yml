name: Linter
on:
  push:
    branches:
      - main

jobs:
  linter:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: Lint Action
        uses: wearerequired/lint-action@v2.1.0
